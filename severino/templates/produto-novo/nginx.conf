# Snippet nginx — subdomínio de produto isolado
# Copie para dentro do server block 443 do /etc/nginx/sites-enabled/severino
# e troque {SLUG} e {PORTA}.

# location / (raiz do subdomínio) — landing + API do produto
# server_name app.{SLUG}.severinobot.com

# Exemplo (cole dentro do server 443, após os outros locations):
#
#   location / {
#     proxy_pass http://127.0.0.1:{PORTA};
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
